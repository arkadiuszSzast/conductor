# Tasks — no-finding-markers

## 1. Agents

- [x] 1.1 [review] Add the delivery narration ban (no `(finding N)`,
  `(code-review nit)`, `(review finding: …)` markers) to
  `~/.config/opencode/agents/conductor-implementer.md` under Repo
  adaptation as point 4.
- [x] 1.2 [review] Add the same ban to
  `~/.config/opencode/agents/conductor-fixer.md` inside the Review-findings
  mode section.

## 2. Validation

- [x] 2.1 [test] Full gate green: `bun run typecheck && bun test`.
